# Streamlit Cloud picks this up at the repo root.
# Runtime deps only — Databricks SDK / psycopg are not used by the
# Streamlit-Cloud demo and are kept for the Databricks bundle path only.

streamlit>=1.57.0
anthropic>=0.102.0
pandas>=3.0.0
plotly>=6.0.0
altair>=6.0.0
pillow>=12.0.0
pypdf>=6.0.0
qrcode>=8.0.0
reportlab>=4.5.0
